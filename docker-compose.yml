version: "3.5"

services:
    git-rate-auth:
        container_name: git-rate-auth
        depends_on:
            - 'postgres-db'
        build:
            context: ./src/backend
            dockerfile: DockerfileAuth
        ports:
            - '5000:5000'
        environment:
            ASPNETCORE_ENVIRONMENT: 'Development'
            
    postgres-db:
        container_name: git-rate-users-db
        image: postgres:latest
        ports:
            - '5432:5432'
        volumes:
            - git-rate-users-db-volume:/var/lib/postgresql/data
        environment:
            POSTGRES_USER: 'postgres'
            POSTGRES_PASSWORD: 'postgres'
            
volumes:
    git-rate-users-db-volume: